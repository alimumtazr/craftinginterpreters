class Animal {
  init(name) { this.name = name; }
  speak() { return this.name + " makes a sound"; }
}
class Dog < Animal {
  speak() { return super.speak() + " (woof)"; }
}
print Dog("Rex").speak(); // expect: Rex makes a sound (woof)
